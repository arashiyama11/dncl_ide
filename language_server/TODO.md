# DNCL Language Server (LSP) TODOリスト

このドキュメントは、`DNCLLanguageServer.kt` における既存のLSP機能の実装状況を評価し、今後の改善点や未実装の機能をまとめたものです。
以下の実装は必ずtwadaの提唱するTDDのプロセスに従い実装をすること。

## ゴール

- 以下の機能が実装され、LSPの基本的な要件を満たすこと。

## 1. 実装が薄い、または簡易的な機能

以下の機能は基本的な動作はするものの、より堅牢な実装や高度な機能追加が必要です。

### 1.1 `textDocument/hover` (Hover機能)

[ ] **現状**: `builtInFunctionDescriptions` に基づく組み込み関数の説明のみを提供。
[ ] **課題**: ユーザー定義の変数、関数、型などに対するホバー情報を提供できていない。より詳細な型情報やドキュメントコメントの表示に対応する必要がある。
[ ] **改善案**: 構文解析結果（ASTやシンボルテーブル）を利用して、カーソル位置のシンボルに対応する定義元や型情報を取得し、ホバーとして表示する。
[ ] **実装状況**: 型の整合性修正完了、ASTとSymbolTableを使用した実装に更新済み

### 1.2 `textDocument/definition` (Go to Definition機能)

[ ] **現状**: `SuggestionUseCase` を利用し、コード内のリテラルとオフセットに基づいて定義元を推測している。
[ ] **課題**:
単純なリテラルマッチングに近く、スコープやセマンティクスを考慮した正確な定義元へのジャンプができていない可能性がある。特に、同名の異なるスコープのシンボルや、複雑なデータ構造内の定義に対���できていない。
[ ] **改善案**: 構文解析結果からシンボルテーブルを構築し、カーソル位置のシンボルがどの定義に対応するかを正確に解決する。

### 1.3 `textDocument/references` (Find References機能)

[ ] **現状**: `Lexer` を利用し、対象トークンのリテラルと一致するすべてのトークンを検索している。
[ ] **課題**: テキストベースの単純な検索であり、セマンティックな参照（スコープ、型、異なる定義だが同じリテラルを持つものなど）を区別できていない。
[ ] **改善案**: シンボルテーブルとASTを利用して、対象シンボルのすべての参照箇所をセマンティックに特定する。

### 1.4 `textDocument/rename` (Rename Symbol機能)

[ ] **現状**: `Lexer` を利用し、対象トークンのリテラルと一致するすべてのトークンを新しい名前に置換している。
[ ] **課題**: `textDocument/references`
と同様に、テキストベースの置換であり、セマンティックなリネームではない。これにより、意図しない箇所がリネームされたり、リネームすべき箇所が漏れたりする可能性がある。
[ ] **改善案**: `textDocument/references` のセマンティックな参照解決を利用し、正確な参照箇所のみをリネームする。

### 1.5 `textDocument/formatting` (Formatting機能)

[ ] **現状**: 各行の先頭と末尾の空白をトリムするだけの非常に簡易的���実装。
[ ] **課題**: DNCLのコードスタイルガイド（もしあれば）に沿った整形や、インデントの自動調整、空白の追加・削除など、本格的なフォーマット機能が欠けている。
[ ] **改善案**: ASTを走査し、DNCLの構文構造に基づいてコードを再構築・整形する。

### 1.6 `textDocument/codeAction` (Code Action / Quick Fix機能)

[ ] **現状**: 「未定義の識別子」に対する「表示」のクイックフィックスのみを提供。
[ ] **課題**: 提供されるコードアクションの種類が非常に限定的。より多様なエラーや�������告に対するクイックフィックス、リファクタリング候補などを提供できる。
[ ] **改善案**: 診断結果やカーソル位置のコンテキストに基づいて、より多くのコードアクション��例:
変数抽出、関数抽出、未使用インポートの削除など）を実装する。

### 1.7 `textDocument/semanticTokens/full` (Semantic Tokens機能)

[ ] **現状**: `getTokenType` 関数が基本的なトークンタイプ（キーワード、変数、数値、文字列、コメント、演���子）に分類している。
[ ] **課題**: 変数と関数、グローバルとローカルなど、より詳細なセマンティックな分類ができていない。こ��により、エディタで���よりリッチなシンタックスハイライトが制限される。
[ ] **改善案**: 構文解析結果やシンボルテーブルを利用して、より粒度の細かいトー��ンタイプ（例:
関数名、パラメータ、クラス名、型名など）を識別し、LSPのセマンティックトークン仕様に沿って提供する。

## 2. 未実装または不明確な機能

## 3. その他

- **エラーハンドリングの強化**: 現在の`handleMessage`内の`try-catch`
  は汎用的。LSPのエラーコードに合わせたより詳細なエラーハンドリングを検討する。
- **テストカバレッジの向上**: 上記の各機能について、より網羅的なユニットテストおよび統合テストを追加する。
- **パフォーマンス最適化**: 大規模なファイルや頻繁な変更に対するパフォーマンス（特に
  `textDocument/didChange`や`publishDiagnostics`）を評価し、必要に応じて最適化��例:
  差分解析、キャッシュ機構の導入）を行う。

## 実装進捗

[x] **型の整合性修正**: SymbolとDnclSymbolの型不整合を修正し、統一されたSymbolクラスを使用
[x] **AstInfoService**: SymbolTableを使用したAST解析とシンボル解決機能を実装
[x] **HoverService**: 新しいSymbol定義を使用するように更新
[x] **DefinitionService**: セマンティックな定義解決機能を実装
[x] **ReferenceService**: セマンティックな参照検索機能を実装
[x] **RenameService**: セマンティックなリネーム機能を実装
[x] **コンパイルエラー修正**: AstVisitorとSymbolTableBuilderの構文エラーを修正
[x] **1.1 Hover機能完了**: ユーザー定義変数・関数・パラメータのホバー情報表示を実装
[x] **1.2 Go to Definition機能完了**: ASTとSymbolTableを使用したセマンティックな定義ジャンプを実装
[x] **1.3 Find References機能完了**: スコープを考慮したセマンティックな参照検索を実装
[x] **1.4 Rename Symbol機能完了**: Find References機能を活用したセマンティックなリネームを実装

### 次の作業項目候補

残りの改善可能な機能：

- **1.5 Formatting機能**: DNCLの構文構造に基づくコー���整形
- **1.6 Code Action機能**: より多様なクイックフィックスとリファクタリング
- **1.7 Semantic Tokens機能**: より詳細なシンタックスハイライト

### 達成した改善効果

**従来の問題点**:

- テキストベースの単純な検索・置換
- スコープやセマンティクスを考慮しない処理
- 組み込み関数の説明のみのホバー情報

**改善後の機能**:

- ASTとSymbolTableによるセマンティック解析
- スコープを正確に考慮した変数・関数の識別
- ユーザー定義シンボルの豊富なホバー情報
- 正確な定義ジャンプと参照検索
- 安全で正確なシンボルリネーム
